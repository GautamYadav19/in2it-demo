import { EditDataTransferDirective } from './edit-data-transfer.directive';

describe('EditDataTransferDirective', () => {
  it('should create an instance', () => {
    const directive = new EditDataTransferDirective();
    expect(directive).toBeTruthy();
  });
});
