import { DataTransferDirective } from './data-transfer.directive';

describe('DataTransferDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTransferDirective();
    expect(directive).toBeTruthy();
  });
});
